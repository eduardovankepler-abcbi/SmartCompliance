import { DatePipe } from '@angular/common';
import { Component, input } from '@angular/core';
import { AuditEntry } from './audit.service';
@Component({selector:'app-audit-trail',imports:[DatePipe],template:`<section class="audit"><header><h2>{{ title() }}</h2><span>{{ subtitle() }}</span></header>@if(entries().length){@for(entry of entries();track entry.id){<article><strong>{{entry.summary}}</strong><time>{{entry.createdAt | date:'short'}}</time><p>{{entry.actorName}} · {{entry.actorRoleKey}}</p><p>{{entry.detail}}</p></article>}}@else{<article><strong>Sem eventos recentes</strong><p>{{emptyMessage()}}</p></article>}</section>`,styles:`.audit{margin-top:24px;padding:20px;background:var(--abc-surface);border:1px solid var(--abc-border);border-radius:8px}.audit header{display:flex;justify-content:space-between;gap:12px}.audit h2{margin:0;font-size:17px}.audit header span,p{color:var(--abc-text-muted)}article{margin-top:12px;padding:12px;border:1px solid var(--abc-border);border-radius:var(--abc-radius)}time{float:right;color:var(--abc-text-muted);font-size:12px}p{margin:6px 0 0;font-size:13px}`})
export class AuditTrailComponent { readonly entries=input<readonly AuditEntry[]>([]);readonly title=input('Trilha de auditoria');readonly subtitle=input('Eventos recentes');readonly emptyMessage=input('Nenhum evento encontrado.'); }
